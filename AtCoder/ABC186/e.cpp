#include <bits/stdc++.h>

using namespace std;

int main(){
    int T;
    cin >> T;

    vector<int> N(T), S(T), K(T);
    for(int i=0; i < T; ++i) cin >> N[i] >> S[i] >> K[i];

    
    return 0;
}
